# Minimal embedded config — just loads the real grub.cfg from the EFI partition
# This is baked into bootx64.efi. Edit /boot/efi/EFI/BOOT/grub.cfg instead.
set root=(hd0,gpt1)
source /EFI/BOOT/grub.cfg
