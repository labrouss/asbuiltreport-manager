#!/bin/sh
# /etc/init.d/S99console
# Previously spawned abr-console via inittab respawn.
# Now handled by /root/.profile after getty login.
case "$1" in
  start|stop|restart) ;;
  *) echo "Usage: $0 {start|stop|restart}"; exit 1 ;;
esac
